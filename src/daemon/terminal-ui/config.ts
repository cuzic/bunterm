/**
 * Terminal UI Configuration Constants
 *
 * Note: Configurable values (font sizes, delays) are in config.yaml under `terminal_ui:` section.
 * This file only contains internal localStorage keys.
 */

/** localStorage key for persisting font size */
export const STORAGE_KEY = 'tui-font-size';

/** localStorage key for onboarding shown flag */
export const ONBOARDING_SHOWN_KEY = 'tui-onboarding-shown';

/** localStorage key for auto-run mode state */
export const AUTO_RUN_KEY = 'tui-auto-run';

/** localStorage key for snippets */
export const SNIPPETS_KEY = 'bunterm-snippets';

/** localStorage key for clipboard history */
export const CLIPBOARD_HISTORY_KEY = 'bunterm-clipboard-history';
