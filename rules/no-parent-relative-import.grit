language js;

`import $_ from $path` where {
  $path <: r"'\.\./.*'",
  register_diagnostic(
    span = $path,
    message = "Avoid parent-relative imports (../). Use the path alias '@/' instead.",
    severity = "error"
  )
}
